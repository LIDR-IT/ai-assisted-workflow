{
  "version": "1.0",
  "description": "Cross-platform hooks for AI-assisted development workflow",
  "_notes": {
    "platforms": "Valid values: cursor, claude, gemini",
    "cursor_limitation": "Cursor hooks are unreliable and lack PreToolUse - use Husky pre-commit instead",
    "sync_behavior": "sync-hooks.sh reads 'platforms' field to generate platform-specific configs"
  },
  "hooks": {
    "notify": {
      "event": "Notification",
      "matcher": "*",
      "command": "bash \"${PLUGIN_ROOT}/hooks/scripts/notify.sh\"",
      "timeout": 5,
      "platforms": ["claude", "gemini"],
      "description": "Desktop notifications for task completion"
    },
    "auto-format": {
      "event": "PostToolUse",
      "matcher": "Edit|Write",
      "command": "bash ${PLUGIN_ROOT}/hooks/scripts/auto-format.sh",
      "timeout": 30,
      "platforms": ["claude", "gemini"],
      "description": "Auto-format files with prettier after edits"
    },
    "protect-secrets": {
      "event": "PreToolUse",
      "matcher": "Edit|Write",
      "command": "bash ${PLUGIN_ROOT}/hooks/scripts/protect-secrets.sh",
      "timeout": 10,
      "platforms": ["claude", "gemini"],
      "description": "Block edits to sensitive files (.env, .key, .pem, etc.)"
    }
  }
}
